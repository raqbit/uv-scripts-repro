[project]
name = "uv-scripts-issue"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
foo = "foo:run"

[tool.uv]
package = true
